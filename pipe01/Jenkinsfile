pipeline {
    agent any


    stages {

        stage("Build") {
            steps { 
		    app = docker.build("miapache")
		    sh 'docker run -d --name apache -p 81:80 miapache'
		} // del step
	} //del stage
    } // del stage
} // del pipeline

